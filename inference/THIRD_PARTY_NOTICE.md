# Third-party components

The `benchmark_networks/` directory is derived from the ToothFairy2 benchmark distribution and retains its included license. The pipeline also depends on nnU-Net, UMambaBot/Mamba, PyTorch, Qwen, Hugging Face Transformers, PEFT, and their transitive dependencies. Model checkpoints and datasets are not redistributed and remain subject to their original terms.
